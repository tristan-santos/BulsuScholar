import { useEffect, useMemo, useState } from "react"
import { toast } from "react-toastify"
import { HiOutlineCheck, HiOutlineClock, HiOutlineEye, HiOutlineX } from "react-icons/hi"
import { listPriorityRecords, reviewLeaveRequest } from "../services/priorityOneService"

export default function AdminLeaveManagement() {
	const [records, setRecords] = useState([]); const [tab, setTab] = useState("pending"); const [selected, setSelected] = useState(null); const [reason, setReason] = useState(""); const [saving, setSaving] = useState(false)
	const load = async () => { const result = await listPriorityRecords("leave_requests"); setRecords(result.records || []) }
	useEffect(() => { load().catch(() => toast.error("LOA records could not be loaded.")) }, [])
	const rows = useMemo(() => records.filter((item) => tab === "all" || item.status === tab), [records, tab])
	const decide = async (decision) => { setSaving(true); try { await reviewLeaveRequest({ requestId: selected.id, decision, reason, actorId: sessionStorage.getItem("bulsuscholar_userId") || "admin" }); toast.success(`Request ${decision}.`); setSelected(null); setReason(""); await load() } catch (error) { toast.error(error.message) } finally { setSaving(false) } }
	return <section className="admin-feature-page"><header className="admin-feature-header"><span><HiOutlineClock /></span><div><h2>Leave and Return Requests</h2><p>Review LOA freezes and returning-student reactivation requests.</p></div></header><div className="admin-feature-stats"><div><span>Pending</span><strong>{records.filter((item) => item.status === "pending").length}</strong></div><div><span>Approved</span><strong>{records.filter((item) => item.status === "approved").length}</strong></div><div><span>Rejected</span><strong>{records.filter((item) => item.status === "rejected").length}</strong></div></div><div className="admin-feature-tabs">{["pending","approved","rejected","all"].map((item) => <button key={item} className={tab === item ? "active" : ""} onClick={() => setTab(item)}>{item}</button>)}</div><div className="admin-feature-table-wrap"><table className="admin-feature-table"><thead><tr><th>Student ID</th><th>Student Name</th><th>Request</th><th>Reason</th><th>Status</th><th>Submitted</th><th>Action</th></tr></thead><tbody>{rows.map((item) => <tr key={item.id}><td>{item.studentId}</td><td>{item.studentName || "-"}</td><td>{item.requestType === "loa" ? "Leave of Absence" : "Return to Study"}</td><td title={item.reason}>{item.reason}</td><td><span className={`leave-status ${item.status}`}>{item.status}</span></td><td>{new Date(item.createdAt).toLocaleDateString()}</td><td><button className="admin-table-view" onClick={() => setSelected(item)}><HiOutlineEye /> View</button></td></tr>)}</tbody></table>{!rows.length ? <div className="portal-empty">No {tab === "all" ? "" : tab} requests.</div> : null}</div>{selected ? <div className="priority-modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && setSelected(null)}><section className="priority-modal"><button className="priority-modal-close" onClick={() => setSelected(null)}><HiOutlineX /></button><header><span><HiOutlineClock /></span><div><small>{selected.requestType === "loa" ? "Leave request" : "Return request"}</small><h2>{selected.studentName || selected.studentId}</h2></div></header><div className="priority-detail-grid"><div><small>Student ID</small><strong>{selected.studentId}</strong></div><div><small>Status</small><strong>{selected.status}</strong></div><div className="wide"><small>Reason</small><strong>{selected.reason}</strong></div>{selected.notes ? <div className="wide"><small>Notes</small><strong>{selected.notes}</strong></div> : null}</div>{selected.document?.url ? <a className="priority-document-link" href={selected.document.url} target="_blank" rel="noreferrer">View supporting PDF</a> : null}{selected.status === "pending" ? <><label>Decision reason / notes<textarea value={reason} onChange={(event) => setReason(event.target.value)} /></label><footer><button className="danger" onClick={() => decide("rejected")} disabled={saving}>Reject</button><button onClick={() => decide("approved")} disabled={saving}><HiOutlineCheck /> Approve</button></footer></> : null}</section></div> : null}</section>
}
